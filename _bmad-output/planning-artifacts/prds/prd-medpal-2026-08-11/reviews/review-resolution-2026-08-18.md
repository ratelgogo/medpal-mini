# 2026-08-18 审查问题处理结果

## 结论

两份独立审查共发现 0 项 Critical、10 项 Major。所有 Critical/Major 已写回 PRD 或以 `[ASSUMPTION]` 明示产品口径；当前没有未处理的定稿阻断项。

## 拉新活动审查处理

| 问题 | 处理结果 |
|---|---|
| M-01 首单定义不稳定 | 已改为：被邀请人在绑定前不得有成功支付的专业陪诊订单；绑定后首笔成功支付以原子操作锁定首单订单 ID；取消后解除，下一笔重新锁定。 |
| M-02 首单券未使用和取消语义 | 已明确：首单支付时可使用，未使用则支付成功后失效；首单取消后解除首单锁定，已使用的券按现有返券规则和券实例状态条件恢复。 |
| M-03 跨订单返券并发 | 已新增优惠券实例 ID、原占用订单 ID 和预期券状态条件迁移；迟到回调不能恢复已被其他订单占用或已核销的券。 |
| M-04 现金到账标准 | 2026-08-18 用户进一步确认：奖励先进入平台余额账户，唯一奖励流水和可用余额增加即为奖励入账成功；提现到绑定微信零钱时，第三方成功回执和渠道流水号作为提现成功判据。 |
| M-05 失败补发与幂等冲突 | 奖励余额入账按唯一奖励动作幂等；提现按唯一提现单号处理，重复请求和迟到回调不得重复冻结、扣减或转账，失败关闭后原额解冻。 |
| M-06 完成后退款影响 | 已明确：首单完成且当时无在处理退款/售后才触发；完成后才发生的售后退款不自动追回已到账奖励，由平台人工处理异常或作弊。 |
| M-07 自邀识别范围 | 已收口为“同账号自邀”；一期不承诺跨账号自然人识别，FR、术语和 CM-006 已统一。 |

原审查的 3 项 Minor 同步处理：SM-010 改为绑定日期 cohort + 内部配置观察窗口；术语表新增首单订单、首次服务优惠券、邀请现金奖励；活动关闭不影响关闭前已建立关系继续履约。

## 就诊人字段审查处理

| 问题 | 处理结果 |
|---|---|
| M1 “特定精神疾病”范围未定义 | 已按 `[ASSUMPTION]` 使用运营后台维护的关注病史项，字段支持“无/不清楚/有 + 具体项/其他说明”；只记录用户或就诊人陈述，不形成诊断、评级或自动处置。 |
| M2 角色可见范围冲突 | 权限矩阵新增字段级一行：当前用户管理；陪诊师在订单生成后看本单快照并核对；运营仅在履约异常/安全事件时看；售后仅在相关 case 确有需要时看。 |
| M3 新建必填与每单复核不一致 | 已明确：已有档案缺字段时下单前补录；当前用户每次下单前确认，订单保存快照；陪诊师只能核对；服务开始前可由当前用户更新并同步，开始后变更只影响后续订单。 |

原审查的 2 项 Minor 同步处理：增加年龄确认日期；明确订单字段快照不自动写入长期健康档案或形成长期健康结论。

## 仍需产品负责人知悉的假设

- 拉新券的发放/使用时点和首单锁定方式是为完成可验收闭环所作的实现口径，已在 PRD 标记 `[ASSUMPTION]`。奖励进入余额账户并可提现到绑定微信零钱已由用户确认；一期不收手续费、不设最低提现金额仍为 `[ASSUMPTION]`。
- “特定精神疾病史”采用后台关注病史项，而不是在本版固定疾病清单，已在 PRD 标记 `[ASSUMPTION]`。

这些假设不阻塞当前 PRD 定稿；若产品负责人后续给出不同口径，应作为 override 更新 PRD 与 memlog。

## 余额账户与提现覆盖审查

2026-08-18 用户将现金直发改为余额账户后，独立财务审查发现 0 项 Critical、4 项 Major，已全部处理：

- 提现单创建、余额校验、冻结和流水写入原子完成，余额不得为负，并发提现不得超额冻结。
- 渠道结果未知时保持冻结；只有明确失败且确认未转账后才解冻。
- 每笔提现绑定唯一稳定渠道转账标识，未知结果只查询原请求，禁止创建新转账；成功终态不可被乱序回调覆盖。
- 售后处理中暂停奖励；售后关闭且未退款后入账，入账前已退款不发，入账后退款转人工复核。

修订后复审结论：**PASS**，遗留 Critical 0 项、Major 0 项。
